export interface StudentProfile {
  branch: string;
  enrollmentNumber: string;
}

export interface StudentDetails {
  branch: string;
  enrollmentNumber: string;
  subject: string;
  examPhase: string;
}
